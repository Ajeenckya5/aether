# Unused. Aether does not sign into WHOOP or pull the WHOOP cloud.
# Connection is Bluetooth (standard HR strap) on this device.
WHOOP_CLIENT_ID=
WHOOP_CLIENT_SECRET=
WHOOP_REDIRECT_URI=http://localhost:3000/api/auth/callback

# Weather, AQI, UV, elevation, and city search use Open-Meteo (no key).
# The browser talks to Open-Meteo directly. Location is rounded (~1 km).
# Your IP is never sent to a locator. Coach cards use wger.de (no key).

# Public website (GitHub Pages). Personal data stays in each visitor's browser.
NEXT_PUBLIC_SITE_URL=https://ajeenckya5.github.io/aether
NEXT_PUBLIC_BASE_PATH=
NEXT_PUBLIC_STATIC=

# Canonical phone install is the live PWA (Add to Home Screen / Chrome Install).
# Play / App Store URLs stay empty until those listings exist — do not invent them.
NEXT_PUBLIC_GITHUB_OWNER=Ajeenckya5
NEXT_PUBLIC_GITHUB_REPO=aether
NEXT_PUBLIC_GITHUB_BRANCH=master
NEXT_PUBLIC_PLAY_STORE_URL=
NEXT_PUBLIC_APP_STORE_URL=
