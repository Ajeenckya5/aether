name: Links

on:
  pull_request:
  push:
    branches: [master]

permissions:
  contents: read

jobs:
  lychee:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: lycheeverse/lychee-action@v2
        with:
          args: --no-progress --exclude 'localhost|127\.0\.0\.1|https://ajeenckya5\.github\.io/aether/sitemap\.xml' public README.md
          fail: true
