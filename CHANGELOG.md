# Changelog

## Unreleased

- Today shows a time-of-day greeting, and the date no longer mismatches the static page.
- Icons are real PNG files, including a maskable icon and an Apple touch icon.
- GPS reverse lookup uses BigDataCloud and falls back to a "Near …" label.
- Recovery age stays hidden until 14 real nights and 6 real markers. Sample history never gets a confidence label.
- Workout and coach films use a self-hosted route drawing instead of remote video.
- Settings can export and import the on-device JSON, and the app asks the browser to persist storage.
- Shared training math lives in `@ajeenckya/engine`.
